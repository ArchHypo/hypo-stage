{
  "buildCommand": "yarn install --ignore-engines && yarn build:standalone",
  "outputDirectory": "dist-standalone",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
