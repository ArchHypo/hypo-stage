{
  "buildCommand": "yarn install --ignore-engines && (test -n \"$VITE_BACKEND_URL\" || (echo 'ERROR: Set VITE_BACKEND_URL in Vercel (Settings → Environment Variables) so the app loads hypotheses from your Render backend instead of static demo data.' && exit 1)) && yarn build:standalone",
  "outputDirectory": "dist-standalone",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
