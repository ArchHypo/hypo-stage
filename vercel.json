{
  "buildCommand": "yarn install --ignore-engines && yarn vercel-build",
  "outputDirectory": "hypo-stage/dist-standalone",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
