{
  "buildCommand": "yarn install --ignore-engines && (test -n \"$VITE_BACKEND_URL\" || (echo 'Set VITE_BACKEND_URL in Vercel env vars.' && exit 1)) && yarn build:types && cd hypo-stage && yarn build:standalone",
  "outputDirectory": "hypo-stage/dist-standalone",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
