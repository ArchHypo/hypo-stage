/**
 * Feature flag name used to enable the Hypo Stage plugin (sidebar, routes, catalog tab).
 * Use this constant in the app for FeatureFlagged and featureFlags config so the same flag is used everywhere.
 */
export const HYPO_STAGE_FEATURE_FLAG = 'hypo-stage';
